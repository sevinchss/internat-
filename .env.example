# Contact form → Telegram. If both are empty, submissions are only logged on the server.
# 1) Create a bot with @BotFather and copy its token
# 2) Add the bot to your group/channel (or message it), then get the chat id, e.g. via
#    https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Public site URL used for canonical links, sitemap and Open Graph
NEXT_PUBLIC_SITE_URL=https://ils.piima.uz
